# HTTP (LAN reachable)
HOST=0.0.0.0
PORT=8787

# SQLite DB location
MISSION_CONTROL_DB_PATH=./data/mission-control.sqlite

# Simple auth (v1)
# If both are set, API routes require Basic Auth.
BASIC_AUTH_USER=jarvis
BASIC_AUTH_PASS=change-me
